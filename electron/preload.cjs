// Minimal preload — no privileged APIs exposed to the web UI.
// The React client continues to talk to the local Express server via HTTP,
// which is already how it works in the browser. Nothing to bridge.
